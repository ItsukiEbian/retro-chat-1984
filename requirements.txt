# Web アプリ
flask
flask-socketio
# Render 用: gunicorn + eventlet で起動
gunicorn
eventlet
# 環境変数（ローカル .env / Render ではダッシュボードの値を優先）
python-dotenv
pyngrok
# 会員制・認証・DB
authlib
flask-login
flask-sqlalchemy
requests
